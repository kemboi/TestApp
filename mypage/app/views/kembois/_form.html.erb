<%= form_for(@kemboi) do |f| %>
  <% if @kemboi.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@kemboi.errors.count, "error") %> prohibited this kemboi from being saved:</h2>

      <ul>
      <% @kemboi.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :address %><br>
    <%= f.text_field :address %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

<%= form_for(@vitabus) do |f| %>
  <% if @vitabus.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@vitabus.errors.count, "error") %> prohibited this vitabus from being saved:</h2>

      <ul>
      <% @vitabus.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :Name %><br>
    <%= f.text_field :Name %>
  </div>
  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :dateborrowed %><br>
    <%= f.date_select :dateborrowed %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
